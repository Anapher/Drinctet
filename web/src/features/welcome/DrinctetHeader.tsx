import * as React from "react";
import Typography from "@material-ui/core/Typography";

export default () => (
  <div>
    <Typography variant="h2">Drinctet</Typography>
    <Typography variant="h6">The goal is to drink that!</Typography>
  </div>
);
